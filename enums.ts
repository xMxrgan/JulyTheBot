export enum CHAT_TYPE {
    PRIVATE= 'private',
    GROUP= 'group',
    SUPERGROUP = 'supergroup',
    CHANNEL = 'channel'
}

export enum MESSAGE_ORIGIN_TYPE {
    USER = 'user',
    HIDDEN_USER = 'hidden_user',
        CHAT ='chat',
            CHANNEL ='channel'
}

export enum MESSAGE_ENTITY_TYPE {

}

export enum KEYBOARD_BUTTON_STYLE {
    PRIMARY = 'PRIMARY',
            SUCCESS= 'success',
    DANGER = 'danger'


}
